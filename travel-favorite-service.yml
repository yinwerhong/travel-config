server:
  port: 8083

spring:
  database:
    driverClassName: com.mysql.jdbc.Driver
  datasource:
    url: jdbc:mysql://localhost:3306/travel-favorite?useUnicode=true&characterEncoding=utf-8
    username: root
    password: 12345678
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
